# خطة مشروع مأوى — نظام الإدارة والأتمتة المتكامل

> مرجع حيّ نرجع له كل جلسة. يحدّث بعد كل مرحلة منجزة.

## 1. نظرة عامة

مشروع **مأوى للتصوير العقاري (MAWA)** — شركة تصوير عقاري يديرها **3 شركاء** هم أنفسهم منفّذو الخدمات:
تصوير فوتوغرافي HDR، فيديو سينمائي، تصوير درون 4K، وجولات افتراضية 360°.

الهدف: نظام مؤتمت بالكامل يدير العقود الإلكترونية، الباقات والأسعار، الشروط، العملاء،
التواصل عبر واتساب، التسويق، وSEO — بأقل تدخّل يدوي ممكن.

**دوري:** بناء وأتمتة النظام المتكامل خطوة بخطوة.

---

## 2. الحالة الحالية (مبني)

| المكوّن | الحالة | ملاحظات |
|---------|--------|---------|
| موقع تسويقي `index.html` | ✅ مبني + SEO | صفحة عامة واحدة، RTL عربية |
| نموذج طلب خدمة `form.html` | ✅ مبني | يُدخِل في `clients` + `contracts` + `contract_services` |
| نظام عقود `admin.html` | ✅ مبني (2372 سطر) | CRUD كامل + تصدير PDF + 8 حالات عقد |
| قاعدة بيانات Supabase | ✅ تعمل | جداول: `clients`, `contracts`, `contract_services` |
| الاستضافة | ⏳ GitHub Pages حالياً | سيُنقل لنطاق خاص |

**قيود البنية الحالية:**
- موقع ثابت بدون خطوة بناء، CSS/JS مضمّنة داخل كل صفحة (غير مشتركة بين الصفحات).
- لا يوجد مصادقة على `admin.html` (فقط `noindex`) — **ثغرة أمنية يجب حلها أولاً**.
- لا توجد قواعد RLS موثّقة على الجداول.

**حالات العقد المعتمدة حالياً:** `draft` → `new` → `awaiting_payment` → `deposit_paid` → `awaiting_signature` → `in_progress` → `fully_paid` → `completed`

---

## 3. القرارات المحسومة

- ✅ **نطاق خاص:** شراء نطاق لمأوى (يستضيف عليه كل شيء). يفضّل `.sa` / `.com.sa` (يتطلب سجلاً تجارياً) أو `.com`.

## 4. القرارات المعلّقة (تحتاج إجابة قبل التنفيذ)

| # | القرار | الخيارات | التوصية |
|---|--------|---------|---------|
| د1 | **اسم النطاق** | `mawa.sa`؟ `mawa-re.sa`؟ غيره؟ | قصير + يدلّ على العلامة |
| د2 | **مزوّد الاستضافة/DNS** | Cloudflare (DNS+Pages+Workers مجاناً) أم GitHub Pages + Supabase Edge Functions؟ | Cloudflare لإدارة DNS والنطاق + أتمتة عبر Workers/Supabase |
| د3 | **تكامل واتساب** | واتساب أعمال API رسمي (Wati / 360Dialog / Twilio / Interakt) | Twilio لتوافقه مع المطورين؛ كلها برسوم شهرية + لكل محادثة |
| د4 | **المدفوعات الإلكترونية** | هل العقد يتطلب دفع/عربون إلكتروني؟ بوابة: Tap / HyperPay / مدى / Apple Pay؟ | Tap شائعة محلياً |
| د5 | **مصادقة الشركاء** | Supabase Auth بـ 3 حسابات (واحد لكل شريك)؟ | نعم — حساب مستقل لكل شريك |
| د6 | **بريد النطاق** | Zoho مجاني / Google Workspace / Cloudflare Email Routing؟ | يحدّد حسب الحجم |
| د7 | **خطة Supabase** | المجانية تكفي للبداية؟ الأتمتة قد تحتاج Pro | ابدأ مجاناً، ارتقِ عند الحاجة |

### بيانات النشاط المؤكّدة (مرجع NAP موحّد للـ GEO)
> مصدر واحد للحقيقة يُنسخ منه في كل مكان (schema، التذييل، sitemap، واتساب).
> أكمل الفراغات تدريجياً.

| الحقل | القيمة |
|-------|--------|
| الاسم التجاري | مأوى للتصوير العقاري (MAWA Real Estate Photography) |
| الاسم القانوني | مؤسسة مأوى المهارة التجارية |
| السجل التجاري | 1009034486 |
| المدينة | **الرياض** ✅ |
| الدولة | المملكة العربية السعودية (SA) |
| الحي/الشارع | ⏳ يحدّد لاحقاً |
| الإحداثيات (lat/long) | ⏳ يحدّد لاحقاً |
| الهاتف | +966531646152 |
| البريد الحالي | Alfaifiwf@gmail.com (← يُغيّر لبريد النطاق) |
| النطاق | ✅ **maawaa.sa** (مملوك + موافق عليه، خوادم Cloudflare: valentin/ximena.ns.cloudflare.com) |
| ساعات العمل | الأحد–الخميس 09:00–18:00 (الجمعة/السبت ⏳) |
| الخدمات | تصوير فوتوغرافي HDR، فيديو سينمائي، درون 4K، جولات 360° |

---

## 5. البنية التقنية المستهدفة

```
النطاق الخاص (DNS على Cloudflare)
├── موقع ثابت: index.html (تسويقي) + صفحات هبوط الحملات
├── form.html (طلب خدمة) ──┐
├── admin.html (لوحة إدارة)──┤
│                           ▼
│               Supabase (Postgres + Auth + Storage)
│               ├── DB: clients, contracts, contract_services,
│               │         services, packages, terms, message_templates...
│               ├── Auth: حسابات الشركاء (RLS)
│               └── Edge Functions: واتساب، الإشعارات، الجدولة
│
└── أتمتة (Supabase Edge Functions / Cloudflare Workers)
    ├── إشعار واتساب تلقائي عند تغيّر حالة العقد
    ├── تذكيرات مجدولة (موعد التصوير، الدفع، التسليم)
    └── حملات رسائل تسويقية
```

**مبادئ التصميم:**
- أبقِ الموقع ثابتاً ما أمكن؛ منطق الخادم في Supabase/Edge Functions فقط.
- كل تكامل خارجي (واتساب، بوابة دفع) خلف دالة (function) قابلة للاستبدال.
- مفتاح anon العام يبقى في الواجهة؛ الأسرار (API keys) في Edge Functions فقط.

---

## 6. المراحل والمخرجات

### المرحلة 0 — الأساس والأمان (قبل أي توسعة) ⚠️
- [x] نطاق خاص **maawaa.sa** (مملوك) + خوادم Cloudflare + هجرة الروابط في الكود + ملف `CNAME`
- [ ] مصادقة Supabase Auth على `admin.html` (3 حسابات شركاء)
- [ ] قواعد RLS على كل الجداول (الكتابة للشركاء فقط، القراءة العامة حسب الحاجة)
- [ ] حماية `form.html` من الإساءة (rate-limit / reCAPTCHA)

### المرحلة 1 — نظام الإدارة الداخلي المتكامل
- [ ] **كاتالوج خدمات** ديناميكي (جدول `services`) — يحل محل القيم الثابتة في الكود
- [ ] **باقات وأسعار** (جدول `packages`) — يربط الخدمة × الباقة × السعر
- [ ] **شروط وأحكام** قابلة للتعديل (جدول `terms`) — تُدمج تلقائياً في PDF العقد
- [ ] **CRM عملاء** — بحث، تاريخ التعامل، تكرار، قيمة العميل
- [ ] **لوحة تحكم وإحصائيات** — عقود حسب الحالة، إيرادات، عملاء جدد، أداء شهري
- [ ] توليد رقم عقد تلقائي (`contract_number`)

### المرحلة 2 — الأتمتة وسير العمل
- [ ] **تكامل واتساب** (Edge Function + مزوّد API)
- [ ] **إشعارات تلقائية** عند كل انتقال في حالة العقد (للعميل + للشريك المسؤول)
- [ ] **تذكيرات مجدولة**: قبل موعد التصوير، تذكير الدفع، إشعار التسليم
- [ ] **حملات رسائل تسويقية**: قوالب + قوائم مستهدفين + جدولة
- [ ] ربط العقد برابط دفع إلكتروني (إن وُجدت بوابة)

### المرحلة 3 — التسويق والنمو
- [ ] **صفحات هبوط** لكل حملة (تتبّع مصدر الطلب)
- [ ] **تحليلات**: Google Analytics + تتبّع التحويلات
- [ ] **SEO مستمر**: سرعة، مدوّنة (اختياري)
- [ ] تكامل Google Business Profile
- [ ] *(GEO مغطّى كمسار مستقل أدناه)*

## 7. مسار إعادة التصميم البصري (مرجع: Five Shades Productions)

تحويل `index.html` من الهوية الفاتحة الزجاجية الحالية إلى **طابع سينمائي داكن فاخر**.

**نظام تصميم الهدف (مستخرج من المرجع):**
| العنصر | المرجع | تكييف مأوى |
|--------|--------|-----------|
| الخلفية | أسود دافئ `oklch(0.1)` | خلفية سينمائية داكنة موحّدة |
| النص | أبيض ناعم `oklch(0.97)` | نص فاتح عربي |
| خط العناوين | Montserrat 900 (لاتدعم العربية) | ⏳ خط عربي عريض سينمائي (يُختار) |
| خط الأساس | Inter | ⏳ خط عربي أساسي |
| اللون المميّز | ذهبي/برونزي دافئ | ⏳ أزرق مأوى الحالي أم ذهبي؟ (قرار) |
| العناوين | ضخمة + تقارب حروف شديد | تكبير وتقارب بحدود دعم العربية |
| التنقّل | صفحة واحدة طويلة + شريط علوي شفاف ثابت | نعم |

**أقسام المرجع المطلوب استيعابها:**
- [ ] Hero بملء الشاشة (فيديو/poster) + زر "شاهد الـ Showreel"
- [ ] قسم "من نحن" + عدّادات إحصائية (سنوات، مشاريع، عملاء، مدن)
- [ ] شبكة خدمات (موجودة — تُعاد تنسيقاً)
- [ ] معرض أعمال مميّز (موجود — يُحسّن بصرياً)
- [ ] **Showreel** + **مكتبة فيديو** (جديد — يتطلّب محتوى فيديو/يوتيوب)
- [ ] **Behind the Scenes** (جديد — يتطلّب صور/فيديو من الكواليس)
- [ ] آراء عملاء + شعارات شركاء (موجودة جزئياً — marquee)
- [ ] نموذج تواصل احترافي (موجود `form.html`)

**قرارات معلّقة لإعادة التصميم:**
- [ ] **المحتوى البصري**: هل يتوفّر فيديو Showreel + لقطات كواليس + حساب يوتيوب؟
      (رُئيت أصول "بنر-يوتيوب/توقيع-يوتيوب" في مجلد التصاميم)
- [ ] **الهوية**: اعتماد الطابع الداكن يعني تغيير كل الأصول المطبوعة الموجودة
      (بطاقات، بروشورات) — تأكيد الرغبة في هذه الانعطافة

---

## 8. مسار GEO (تحسين محركات التوليد)

تحسين الظهور في محركات الذكاء الاصطناعي (ChatGPT، Perplexity، Gemini،
Google AI Overviews). يمتد عبر كل المراحل لأنه تراكمي.

**ما هو مبني حالياً (قاعدة جيدة):**
- ✅ LocalBusiness + Organization + WebSite + BreadcrumbList + OfferCatalog (4 خدمات)
- ✅ FAQPage بـ 5 أسئلة بإجابات واقعية ومرقّمة (النسب، المدد)
- ✅ HTML دلالي (تسلسل h1→h2→h3→h4 سليم)
- ✅ `robots.txt` + `sitemap.xml`

**الفجوات ذات الأثر الأعلى (مرتّبة):**
- [ ] **هجرة النطاق في كل البيانات المنظّمة**: كل `@id` و`url` و`canonical` و`og:url`
      و`item` تشير لـ `maawaa.sa` — يجب تحديثها للنطاق الجديد فور امتلاكه
- [ ] **إكمال عنوان النشاط (NAP)**: العنوان الحالي = "السعودية" فقط! أضف المدينة (الرياض)
      والحي والشارع + إحداثيات `geo` (lat/long) — أساسية للظهور المحلي والخرائط
- [ ] **`areaServed`**: حدّد منطقة الخدمة صراحةً (الرياض، ثم مدن أخرى)
- [ ] **`aggregateRating` + `Review`**: قسم آراء العملاء موجود لكن بدون schema —
      أضف تقييماً مجمّعاً ومراجعات منظّمة (AI يقتبس النجوم والأرقام)
- [ ] **أسعار صريحة في `Offer`**: الباقات والأسعار موجودة في النص لكن ليست منظّمة —
      أضف `price` + `priceCurrency` (الأرقام المحدّدة تُقتبس أكثر)
- [ ] **`llms.txt`**: ملف موجّه لنماذج اللغة (كـ robots.txt للذكاء الاصطناعي) يلخّص
      الخدمات والكيان وأهم الروابط
- [ ] **توسيع `sameAs`**: أضف Google Business Profile وحسابات التواصل لربط الكيان
- [ ] **إحصاءات أصيلة قابلة للاقتباس**: مثل "صوّرنا +500 عقار" أو "خبرة +X سنة" —
      أرقام حقيقية تنفرد بها مأوى يرفعها AI في الإجابات
- [ ] **صفحات خدمة مستقلة عميقة**: بدل صفحة واحدة، صفحة لكل خدمة (تصوير عقاري الرياض،
      تصوير درون...) — GEO يفضّل الصفحات المتخصّصة القابلة للفهرسة والاقتباس
- [ ] **`openingHoursSpecification` كاملة**: الحالية "Su-Th 09:00-18:00" ناقصة الجمعة/السبت
- [ ] **`HowTo` schema**: لاستفسارات "كيف أحجز/كيف تتم عملية التصوير"

**أخطاء سريعة (إصلاح فوري):**
- [ ] خطأ مطبعي في FAQ السؤال 5: `"caso by caso"` (إسبانية) بدل عربي — `index.html:2491`
- [ ] توحيد صيغة الرقم: `+966531646152` في schema مقابل `966531646152` في `wa.me`

---

## 9. سجل التقدّم

| التاريخ | الإنجاز | المرحلة |
|---------|---------|---------|
| 2026-08-09 | إنشاء AGENTS.md + PROJECT.md، تأسيس الخطة + مسار GEO | — |
| 2026-08-09 | إضافة مسار إعادة التصميم (مرجع Five Shades Productions) | إعادة تصميم |
| 2026-08-09 | **إعادة بناء `index.html` كاملة** بالطابع السينمائي الداكن (ذهبي/برونزي + Cairo/Tajawal) — أقسام: Hero بملء الشاشة، من نحن + إحصائيات، خدمات، **أسعار (قسم ظاهر بدل overlay)**، أعمال، Showreel، مكتبة فيديو، كواليس، رحلة العمل، آراء عملاء (marquee)، شركاء، FAQ، تواصل. تم إصلاح `addressLocality` (السعودية→الرياض) + `areaServed` + `openingHoursSpecification` + خطأ `caso by caso` | إعادة تصميم |
| 2026-08-09 | **مطابقة نظام ألوان وتقسيم Five Shades**: لوحة أحادية اللون دافئة (داكن `#161311` + أبيض ناعم + رمادي دافئ `#B2AAA0` بدل الذهبي)، فواصل hairline + رفع alpha بدل ألوان صلبة، عناوين eyebrow وزن 400، أزرار monochrome عالية التباين، خلفية Hero بصورة حقيقية + تعتيم | إعادة تصميم |
| 2026-08-09 | تبسيط نظام الألوان لثيم **سوداء/عاجي + أحمر إشارة**: `--black #020001`، `--black-secondary #101010`، `--burgundy #2A1518`، `--red #FF3637`، `--white #F4F0EB`، `--gray #A19B97`، `--border #292626`، `--pure-black`؛ غلاف بصورة `cover@4x.png` + تعتيم | إعادة تصميم |
| 2026-08-09 | خطوط IBM Plex Sans Arabic + Sans؛ الغلاف نموذج «مركزي كلاسيكي» بعنوان مكبّر | إعادة تصميم |
| 2026-08-09 | **حاسبة أسعار ذكية** (`calculator.html`): تسعير ديناميكي حسب النوع/الغرف + باقة + إضافات + تصميم، **خصم باقة مجمّعة**، **خصم وحدات متعدّدة سلّمي** (نفس الموقع 10/15/20٪، مواقع مختلفة 5/8/10٪)، نوعا **قصر/مزاد** (حسب المشروع)، عرض سعر رسمي PDF/طباعة (قبل/بعد الخصم، اسم الشركة، شروط، توقيع)، حفظ في Supabase، QR، روابط مشاركة | ميزة |
| 2026-08-09 | **قسم خدمات فاخرة (قصور + مزادات)** في `index.html` بثيم برونزي فاخر يربط بالحاسبة (`?t=palace`/`?t=auction`) | إعادة تصميم |

### محتوى معلّق لإعادة التصميم (أماكن واضحة في الكود)
- [x] `data-id` في قسم الشوريل → فيديو يوتيوب `f_4_bkjH7ZI` (فيلا فاخرة المهندية)
- [x] روابط يوتيوب فعلية في مكتبة الفيديو (6 بطاقات بقيم حقيقية + صور مصغّرة)
- [x] روابط حقيقية لـ Instagram / YouTube / X / Snapchat في التذييل (`@Maawaasa`) + `sameAs` في الـ schema
- [ ] صور حقيقية لقسم الأعمال (`work-grad`) وكواليس (`bts-shot`) — حالياً تدرّجات لونية

### حسابات التواصل المؤكّدة (كلها `@Maawaasa`)
- يوتيوب: https://www.youtube.com/@Maawaasa
- انستقرام: https://instagram.com/Maawaasa
- إكس/تويتر: https://x.com/Maawaasa
- سناب شات: https://snapchat.com/add/Maawaasa

---

## ملاحظات للمحافظة على الجودة
- الخطوط: **IBM Plex Sans Arabic** (عربي) + **IBM Plex Sans** (إنجليزي) من Google Fonts، عبر `--font-display` و`--font-body`. أقصى وزن 700.
- لوحة الألوان **سينمائية بلون إشارة أحمر** (موحّدة كتوكنات في `:root`):
  `--black #080808`، `--black-secondary #101010` (أقسام متناوبة)، `--burgundy #2A1518` (تعتيم الـHero)، `--red #F23846` (**لون إشارة فقط ~3%**: كلمة Hero، زر CTA، تفاصيل)، `--white #F4F0EB`، `--gray #A19B97`، `--border #292626`، `--pure-black #000000`. لا تُبدّد الأحمر.
- كل صفحة مكتفية ذاتياً (CSS/JS مضمّنة) — عند تغيير تصميم، حدّث كل الصفحات.
- رسائل الـ git بالعربية. النشر = `git push origin main`.
