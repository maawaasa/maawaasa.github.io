# نظام تصميم مأوى

> الحالة: مسودة مرجعية بانتظار اعتماد المالك  
> تاريخ الاستخراج: 2026-08-29  
> النطاق: الموقع، صفحات التشغيل، رسائل البريد، العقود وملفات PDF المرتبطة بها  
> مبدأ العمل: هذه الوثيقة تصف الهوية القائمة وتوحد استعمالها؛ لا تمنح إذنًا لتغيير منطق الإرسال أو Supabase أو العقود.

## 1. مصادر المرجع

استُخرج هذا النظام من الملفات الحالية الآتية:

- `approve-contract.html`: صفحة الاعتماد ومستند العقد وملخصه المالي.
- `email-booking-confirmation-preview.html`: معاينة بريد تأكيد الحجز في الوضعين الفاتح والداكن.
- `supabase/functions/contract-approval/index.ts`: قالب بريد العقد الفعلي `buildContractEmail`، وهو ملف محمي لا يُعدّل دون موافقة صريحة.
- `supabase/functions/schedule-change/index.ts`: رسائل طلب تعديل الموعد وقراراته.
- `supabase/functions/operations-automation/index.ts`: رسالة توزيع طلب التصوير على المصور.
- `schedule-change.html` و`assignment-response.html` و`verify.html`: صفحات الحالة والقرار والتحقق.
- أصول الشعار والختم داخل `assets/`، وبالأخص `assets/logo2026/` و`assets/stamp-mawaa*.svg`.

عند التعارض بين هذه الوثيقة وملف قديم، تُعرض الحالة على المالك قبل تعديل الملف. لا يُفترض أن التطبيق القديم يصبح صحيحًا لمجرد وجوده.

## 2. المبادئ الملزمة

1. الاسم الرسمي في جميع الواجهات والمراسلات والعقود هو **مؤسسة مأوى المهارة التجارية**.
2. الهوية عربية و`RTL` من الجذر، مع عزل القيم الرقمية واللاتينية في اتجاه `LTR`.
3. الأولوية لسرعة التصنيف من أول نظرة: الحالة، رقم العقد، الموعد، العميل والمبلغ تظهر في كتل واضحة لا في فقرة طويلة.
4. لا يُستخدم الإيموجي للزخرفة. الرمز البسيط `✦` الظاهر في العقد مقبول كفاصل قسم، ويُستخدم باعتدال وباتساق.
5. يجب أن تعمل الواجهة في الوضعين الفاتح والداكن، وعلى Gmail وApple Mail والآيفون والأندرويد والكمبيوتر، مع اختبار فعلي قبل النشر.
6. لا تُغيّر هذه الوثيقة أي منطق لإرسال البريد أو توليد العقد أو تخزينه أو مزامنته مع Notion وSupabase.

## 3. الخط

### الخط المعتمد

- العائلة الوحيدة المعتمدة للنص العربي والواجهة: **IBM Plex Sans Arabic**.
- الأوزان المعتمدة: `400` للنص العادي و`600` للعناوين والقيم والأزرار والحالات المهمة.
- لا يُستبدل بخط عربي آخر، ولا تُستخدم أوزان `700` أو `800` أو `900` في تنفيذ جديد.
- الأرقام المعزولة تبقى ضمن العائلة نفسها متى كان العميل يدعمها. يجوز وجود fallback تقني في CSS فقط إذا فرضه نظام التشغيل أو عميل البريد، وليس بوصفه اختيارًا بصريًا بديلًا.

```css
font-family: "IBM Plex Sans Arabic", sans-serif;
font-weight: 400; /* نص */
font-weight: 600; /* عنوان أو تأكيد */
```

تحميل الويب المعتمد:

```html
<link
  href="https://fonts.googleapis.com/css2?family=IBM+Plex+Sans+Arabic:wght@400;600&display=swap"
  rel="stylesheet"
>
```

في البريد، يُصرّح بالخط المعتمد حيث يدعم عميل البريد خطوط الويب. إذا أسقط Gmail أو Apple Mail الخط الخارجي، فالخط الظاهر الناتج fallback تقني من العميل، وليس تغييرًا للهوية في المصدر.

## 4. الألوان

### الألوان الأساسية الملزمة

| الرمز | القيمة | الاستخدام |
|---|---:|---|
| `--mawa-red` | `#A4243B` | الأحمر الرسمي، الشريط العلوي، الإجراء الأساسي، عنوان أو إبراز العلامة |
| `--mawa-paper` | `#FBFAF6` | خلفية الورق والعقد والأسطح الدافئة |
| `--mawa-ink` | `#1D1815` | النص الأساسي والعناوين |
| `--mawa-gray` | `#6B6661` | النص الثانوي والتوضيحات والتسميات |
| `--mawa-border` | `#E8E3DA` | الحدود والفواصل |

```css
:root {
  --mawa-red: #A4243B;
  --mawa-paper: #FBFAF6;
  --mawa-ink: #1D1815;
  --mawa-gray: #6B6661;
  --mawa-border: #E8E3DA;
}
```

لا تُنشأ درجات أساسية بديلة قريبة مثل `#211E1C` أو `#FBFAF7` أو `#E5E0DA` في تنفيذ جديد؛ هذه درجات حالية متفرقة موثقة في قسم الاختلافات وتحتاج ترحيلًا منضبطًا بعد الموافقة.

### ألوان الحالة الدلالية

| الدلالة | اللون الحالي المرشح للتوحيد | أمثلة الاستخدام |
|---|---:|---|
| نجاح / قبول / استلام / تأكيد / سداد | `#18794E` | تم اعتماد العقد، استلام العربون، قبول الموعد، قبول المصور، اكتمال السداد |
| معلق / مرفوض / يحتاج إجراء | `#C9364F` | مبلغ معلق، فشل أو رفض، رابط غير صالح، قرار مطلوب |
| إعادة توزيع الطلب / رفض المصور | `#B07C1F` | انتقال الطلب للمصور التالي أو رفض المصور للتكليف |

الأخضر والأحمر والبني ألوان دلالية لا زخرفية. لا يعتمد المعنى على اللون وحده؛ يجب أن يصاحبه نص صريح مثل «تم الاستلام»، «معلق» أو «أعيد التوزيع».

درجات الخلفية الفاتحة الموجودة والمقبولة للحالات:

- نجاح: خلفية `#E9F7EF` مع نص `#18794E`.
- خطر أو رفض: خلفية `#FBECEF` مع نص `#C9364F`.
- إعادة توزيع: `rgba(176, 124, 31, 0.12)` مع نص `#B07C1F`.
- إبراز العلامة: خلفية `#F7EFF0` مع نص `#A4243B`.

### الوضع الداكن

القيم الآتية مستخرجة من معاينة البريد وصفحة تعديل الموعد الحالية:

| الوظيفة | القيمة الداكنة |
|---|---:|
| خلفية التطبيق | `#11100F` |
| سطح الرسالة | `#191715` |
| البطاقة / الحقل | `#211F1D` |
| النص الأساسي | `#F6F2ED` |
| النص الثانوي | `#B5ADA6` |
| الحد | `#3D3936` |
| خلفية التذييل | `#121110` |
| نجاح داكن | نص `#72D89D` على خلفية `#163A2B` |
| خطر داكن | `#FF7B7B` |

في الوضع الداكن تبقى دلالة الحالة نفسها، ويُرفع التباين فقط. لا تُعكس ألوان الشعار داخل الملف ولا تُطبّق عليها فلاتر CSS.

## 5. الاسم والأرقام والبيانات المرجعية

### الاسم الرسمي

يُكتب كاملًا هكذا دون تغيير:

> مؤسسة مأوى المهارة التجارية

لا يُستخدم «المهارية». يجوز استخدام «مأوى» وحدها داخل عنوان قصير عندما يكون الاسم الرسمي ظاهرًا في التذييل أو السياق القانوني نفسه.

### الأرقام المالية

- تُستخدم أرقام إنجليزية وفاصل آلاف إنجليزي: `1,850 ر.س`.
- الاتجاه `LTR` يشمل الرقم والوحدة معًا عند احتمال انقلاب الترتيب.
- لا تُستخدم الأرقام الهندية العربية في القيم المالية.
- التقريب وعدد المنازل العشرية يتبعان قيمة المصدر، بحد أقصى منزلتين كما في القوالب الحالية.

مثال آمن:

```html
<bdi dir="ltr" class="ltr">1,850 ر.س</bdi>
```

### رقم العقد الموحد

- `contract.contract_number` في Supabase هو المرجع المرئي الواحد لرقم العقد.
- القيمة نفسها، دون إعادة توليد أو تنسيق مستقل، تظهر في الموقع والبريد والعقد وNotion وSupabase.
- يجوز تنظيف الرقم لاسم الملف أو مسار التخزين تقنيًا، لكن لا يتغير الرقم المعروض للمستخدم.
- يُعزل الرقم كاملًا بعنصر `bdi` أو `span` ذي `dir="ltr"` و`unicode-bidi:isolate`.
- لا يُكسر رقم العقد على سطرين.

## 6. اتجاه النص ومنع انقلاب المحتوى

قاعدة الصفحة:

```html
<html lang="ar" dir="rtl">
```

القيم التالية تُعزل في عناصر `LTR` مستقلة:

- رقم العقد والطلب.
- المبالغ والنسب المئوية.
- رقم الجوال والسجل والهوية.
- البريد الإلكتروني واسم المستخدم مثل `@maawaasa`.
- روابط الويب.
- التاريخ والوقت إذا كُتبا بأرقام إنجليزية.

```css
.ltr {
  direction: ltr;
  unicode-bidi: isolate;
  font-variant-numeric: tabular-nums;
}
```

قواعد علامات الترقيم:

1. تُعزل القيمة كاملة، لا الأرقام وحدها.
2. النسبة تُعزل مع علامة `٪`: `<bdi dir="ltr">25٪</bdi>`.
3. يفضّل تجنب الأقواس حول محتوى مختلط الاتجاه. عند الحاجة توضع الأقواس داخل عنصر العزل نفسه.
4. توضع النقطتان والفاصل `·` خارج العنصر المعزول إذا كانا يفصلان بين حقول مستقلة.
5. لا تعتمد على `text-align` وحده لمعالجة اتجاه الأرقام.
6. يُمنع إدخال محارف اتجاه مخفية يدويًا داخل البيانات القادمة من Supabase أو Notion؛ العزل مسؤولية العنصر الحاوي.

## 7. أصول الشعار والختم

### أصول 2026 المعتمدة

| الأصل | النسبة الأصلية | الاستخدام |
|---|---:|---|
| `assets/logo2026/mawa-wide.svg` | `473.76 × 88.51`، نحو `5.35:1` | شعار أفقي داكن على خلفية فاتحة |
| `assets/logo2026/mawa-wide-white.svg` | `473.76 × 88.51`، نحو `5.35:1` | شعار أفقي فاتح على خلفية داكنة |
| `assets/logo2026/mawa-wide-white-email.png` | `378 × 137`، نحو `2.76:1` | بديل نقطي للبريد فقط عند تعذر SVG، ولا يُمدد إلى نسبة الشعار الأفقي SVG |
| `assets/logo2026/mawa-tall.svg` | `1422.86 × 1107.14`، نحو `1.29:1` | تكوين رأسي داكن |
| `assets/logo2026/mawa-tall-white.svg` | `1422.86 × 1107.14`، نحو `1.29:1` | تكوين رأسي فاتح |
| `assets/logo2026/mawa-mark.svg` | `501.58 × 249.5`، نحو `2.01:1` | العلامة وحدها والعلامة المائية |
| `assets/stamp-mawaa.svg` | `793.66 × 793.66`، `1:1` | الختم المستقيم |
| `assets/stamp-mawaa-tilted.svg` | `793.66 × 793.66`، `1:1` | الختم المائل المعتمد في العقد |

قواعد الاستخدام:

- استخدم الأصل المناسب للخلفية؛ النسخة البيضاء على الداكنة والنسخة الداكنة على الفاتحة.
- حافظ دائمًا على `width:auto` أو `height:auto` وعلى نسبة العرض إلى الارتفاع الأصلية.
- يُمنع الضغط أو القص أو تغيير النسب أو تدوير الشعار أو إعادة تلوينه أو إضافة ظل عليه.
- لا يوضع الشعار داخل مساحة أصغر من أن تحافظ على وضوح تفاصيله.
- في البريد الحالي، العرض المرجعي للشعار الأفقي `170px`، ويصغر إلى نحو `132px` على الهاتف عند الحاجة.
- العلامة المائية في العقد تستخدم `mawa-mark.svg` بشفافية منخفضة تقارب `0.05`، ولا تستعمل بدل الشعار الرئيس.
- الختم عنصر قانوني بصري: لا يعاد رسمه ولا يغيّر لونه أو نصه أو نسبته.
- ملفات `assets/logo.svg` و`assets/logo-mark*.svg` أصول أقدم مستخدمة في صفحات قائمة؛ لا تستبدلها جماعيًا قبل قرار ترحيل منفصل.

## 8. مكونات التصميم

### 8.1 الهيدر

هيدر البريد والصفحات التشغيلية:

- شريط علوي أحمر رسمي بارتفاع `5px`.
- مساحة شعار داكنة `#090808`، وشعار `mawa-wide-white.svg` في المنتصف.
- حشوة رأسية تقارب `22–24px`.
- لا يضاف نص ملاصق للشعار داخل مساحة الهوية.

هيدر العقد:

- بطاقة ورقية بيضاء بحد `#E8E3DA` وحد علوي أحمر.
- الشعار الأفقي الداكن في المنتصف.
- شارة نوع المستند ورقم العقد والتاريخ أسفل الشعار.

### 8.2 شارات الحالة

- الشكل كبسولة: `border-radius: 999px`.
- الحشوة المرجعية `7px 11–14px`.
- النص `11–12px` أو ما يعادله، بوزن `600`.
- صياغة الشارة تبدأ بالنتيجة: «تم اعتماد العقد»، «المبلغ معلق»، «أعيد توزيع الطلب».
- رقم العقد داخل الشارة يعزل `LTR`.

### 8.3 بطاقات المعلومات

- سطح فاتح: أبيض فوق ورق `#FBFAF6`.
- حد `1px solid #E8E3DA`.
- نصف قطر `12px` للبطاقة المعتادة، و`14px` لبطاقة البريد الواسعة.
- الحشوة المعتادة `16–20px`.
- التسمية صغيرة ورمادية، والقيمة أسفلها بلون الحبر ووزن `600`.
- تُستخدم شبكة عمودين أو ثلاثة على الكمبيوتر، وتتحول إلى عمود واحد على الهاتف.
- لا تُجمع بيانات العميل والموعد والمال في بطاقة نصية واحدة إذا كان فصلها يسرع القراءة.

### 8.4 الجداول

للعقد:

- رأس أحمر `#A4243B` ونص أبيض بوزن `600`.
- جسم أبيض بحدود `#E8E3DA`.
- صفوف زوجية بخلفية ورقية خفيفة.
- المبالغ في عمود مستقل، محاذاة واضحة وعزل `LTR`.

للبريد:

- تستخدم جداول `role="presentation"` للبنية لضمان التوافق.
- الخصائص المهمة توضع inline مع `bgcolor` عند الحاجة.
- لا يعتمد قالب البريد على Flexbox أو Grid أو JavaScript.
- على الهاتف تتحول الأعمدة المتعددة إلى كتل بعرض `100%` دون ضغط النص.

### 8.5 الأزرار

- الزر الأساسي: خلفية `#A4243B`، نص أبيض، نصف قطر `8–10px`، حشوة رأسية `12–14px`.
- زر النجاح أو القبول: `#18794E`.
- زر الرفض العام أو الحالة التي تحتاج إجراء: `#C9364F`.
- زر رفض المصور أو إعادة التوزيع: البني الدلالي بعد اعتماد قيمته النهائية.
- الزر الثانوي: خلفية بيضاء وحد `#E8E3DA` ونص بلون الحبر.
- حالة التعطيل تقلل العتامة، ولا تزال تسمية الزر مقروءة.
- نص الزر فعل واضح: «اعتماد وإرسال»، «الموافقة على الموعد»، «طلب تعديل الموعد».

### 8.6 التنبيهات

- نجاح: خلفية خضراء فاتحة ونص أخضر.
- خطأ أو رفض أو إجراء مطلوب: خلفية حمراء فاتحة ونص أحمر.
- إعادة توزيع: خلفية بنية فاتحة ونص بني.
- نصف القطر `8–12px` والحشوة `12–16px`.
- يبدأ التنبيه بخلاصة القرار ثم التفاصيل أو الإجراء التالي.
- لا يُستخدم لون تحذير أصفر مستقل ما لم تُعتمد له دلالة لاحقًا.

### 8.7 الملخص المالي

- يعرض بحد أقصى أربع قيم، والأصل: الإجمالي، المدفوع/العربون، المعلق/المتبقي.
- القيمة المالية أكبر من التسمية وبوزن `600`.
- المدفوع أو المستلم أخضر، والمعلق أحمر.
- الإجمالي النهائي يمكن أن يظهر على سطح حبر داكن `#1D1815` مع نص ورقي `#FBFAF6` كما في العقد.
- كل مبلغ يعزل `LTR` ويستخدم صيغة مثل `1,850 ر.س`.
- على الهاتف تتكدس الخلايا رأسيًا أو تصبح صفوفًا كاملة؛ لا تصغر الأرقام حتى تفقد قابلية القراءة.

### 8.8 التذييل الموحد

النص الإلزامي، بهذا الترتيب:

> مؤسسة مأوى المهارة التجارية  
> info@maawaa.sa · @maawaasa · [www.maawaa.sa](http://www.maawaa.sa)

- الاسم الرسمي في سطر مستقل.
- البريد والحساب والرابط في سطر ثانٍ، وكل قيمة لاتينية معزولة `LTR`.
- في HTML تكون القيم روابط قابلة للنقر: `mailto:` وInstagram و`https://www.maawaa.sa`.
- خلفية التذييل الفاتح مستخرجة من القوالب الحالية: `#ECE8E3`، والنص رمادي.
- يجوز إضافة جملة «هذه رسالة آلية مرتبطة بطلبك لدى مأوى» بعد التذييل الأساسي، لا بدلًا منه.

### 8.9 المسافات

مقياس الويب والبريد:

| الرمز | القيمة | الاستخدام |
|---|---:|---|
| `space-1` | `4px` | فصل دقيق داخل عنصر |
| `space-2` | `8px` | بين تسمية وقيمة أو بين عناصر صغيرة |
| `space-3` | `12px` | حشوة صغيرة أو فجوة أزرار |
| `space-4` | `16px` | حشوة بطاقة أساسية |
| `space-5` | `20px` | حشوة بطاقة مريحة |
| `space-6` | `24px` | فصل الأقسام الصغيرة والهيدر |
| `space-8` | `32px` | حشوة الرسالة على الكمبيوتر |

- تستخدم مضاعفات `4px` في تنفيذ جديد.
- تنخفض حشوة البريد الأفقية من `32px` إلى `18px` تقريبًا على الشاشات الضيقة.
- في مستند A4 تبقى القياسات بالملليمتر كما في العقد الحالي، ولا تُحوّل آليًا إلى بكسل.

### 8.10 الزوايا والحدود والظلال

- زر أو حقل: `8–10px`.
- بطاقة معلومات: `12px`.
- بطاقة بريد داخلية: `14px`.
- حاوية البريد أو اللوحة الرئيسة: `20px`.
- شارة: `999px`.
- الحد الافتراضي: `1px solid #E8E3DA`.
- الظل خفيف ودافئ، ويُحذف في الطباعة والوضع الداكن إذا قلل الوضوح.
- مستند العقد نفسه شبه مربع؛ زواياه صغيرة ولا يأخذ شكل بطاقة تطبيق.

### 8.11 الخطوط الفاصلة والزخرفة

- الخط الأساسي `1px` بلون الحدود.
- الشريط الأحمر أعلى البريد `5px`.
- الحد العلوي الأحمر للبطاقات المهمة يستخدم باعتدال.
- `✦` فاصل قسم قائم في العقد، بلون الأحمر الرسمي، وليس إيموجي حالة.
- لا تستخدم مجموعة أيقونات أو رموز مختلفة عشوائيًا داخل الرسالة الواحدة.

## 9. البريد والتوافق عبر الأجهزة

1. يبنى البريد بجداول عرض، وخصائص inline، وعروض قصوى `640–650px`.
2. الصور لها `display:block` و`height:auto` و`max-width:100%` وبدون قص.
3. اللون الحرج يكرر في `background` و`background-color`، ويستخدم `bgcolor` عند الحاجة لدعم العملاء القديمة.
4. يوجد preheader مخفي يلخص النتيجة ولا يكرر العنوان حرفيًا.
5. لا تعتمد الرسالة على صورة لعرض معلومة أساسية.
6. الروابط والأزرار لا تقل مساحة لمسها عن نحو `44px` ارتفاعًا على الهاتف.
7. تختبر العربية والأرقام والأقواس في Gmail وApple Mail على iPhone وAndroid وسطح المكتب.
8. لا تستخدم SVG في البريد إلا بعد التحقق من العميل المستهدف؛ الأصل النقطي المخصص للبريد هو fallback، مع الحفاظ على نسبته الأصلية وعدم تمديده.
9. الوضع الداكن يجب أن يكون مقصودًا: تُحدد خلفيات الأسطح والنصوص والحالات ولا يترك العميل يعكس جزءًا من الرسالة فقط.
10. إذا تعارض دعم الوضع الداكن مع سلامة الشعار أو القراءة في عميل بعينه، يوثق الاختبار ويعرض قرار التوافق على المالك قبل تغيير القالب الفعلي.

## 10. الوصول وسرعة التصنيف

- تباين النص الأساسي والزر والحالة يجب أن يبقى واضحًا في الوضعين.
- كل حقل له تسمية نصية؛ لا تستخدم اللون أو الموضع وحدهما.
- ترتيب المعلومة في الرسائل التشغيلية: الحالة ← رقم العقد ← الموعد ← العميل/الموقع ← الخدمات ← الملخص المالي ← الإجراء التالي.
- عناوين الأقسام قصيرة ومباشرة.
- الفقرة الإرشادية لا تتجاوز فكرة واحدة كلما أمكن.
- الأزرار المهمة لا تتنافس بصريًا: إجراء أساسي واحد، والبقية ثانوية أو دلالية.
- لا يخفى التركيز بلوحة المفاتيح، ولا تزال الحالات مفهومة عند تعطيل الصور.

## 11. الاختلافات المرصودة وقرارات التوحيد المطلوبة

لم تُعدّل الملفات التالية. هذه الاختلافات موثقة لقرار المالك:

### 11.1 الخط والأوزان

- `approve-contract.html` و`verify.html` يستخدمان IBM Plex Sans Arabic، لكنهما يحملان أوزان `500` و`700` أيضًا.
- قوالب البريد وصفحة تعديل الموعد وصفحة رد المصور تستخدم `Tahoma, Arial`، وبعضها يطلب أوزان `800` و`900`.
- القرار المقترح: توحيد المصدر على IBM Plex Sans Arabic بوزني `400` و`600` فقط، مع بقاء fallback تقني يفرضه عميل البريد.

### 11.2 درجات الألوان الأساسية

- العقد يستخدم القيم الأساسية المطلوبة: `#A4243B` و`#FBFAF6` و`#1D1815` و`#6B6661` و`#E8E3DA`.
- البريد والصفحات التشغيلية تستخدم درجات قريبة مثل `#FBFAF7` و`#211E1C` و`#746D67` و`#E5E0DA`.
- القرار المقترح: اعتماد القيم الخمس الأساسية في التنفيذ الجديد، ثم ترحيل الدرجات القريبة ملفًا ملفًا بعد معاينة واختبار، لا باستبدال شامل أعمى.

### 11.3 ألوان الحالات

- النجاح يظهر بعدة درجات: `#18794E` و`#16663A` و`#167047` و`#1F8A4C`.
- الرفض أو الخطر يظهر بـ`#C9364F` و`#8D2335` و`#C0392B`.
- لوحة الإدارة الحالية تحتوي لون تحذير بني/ذهبي `#B07C1F`، بينما زر رفض المصور في رسالة التوزيع محايد بخلفية `#EEE9E4` ونص خمري `#7D2637`.
- القرار المقترح: `#18794E` للنجاح، `#C9364F` للمعلق/المرفوض/الإجراء، و`#B07C1F` حصريًا لإعادة توزيع الطلب أو رفض المصور. اعتماد اللون البني يحتاج موافقة صريحة لأنه غير مطبق حاليًا على زر رفض المصور.

### 11.4 الوضع الداكن للبريد

- `email-booking-confirmation-preview.html` يعلن `light dark` ويحتوي تنسيقات داكنة.
- قالب العقد الفعلي في `buildContractEmail` يعلن `only light` لتثبيت مظهره في عملاء البريد.
- `schedule-change.html` يدعم الوضع الداكن، بينما رسائل `schedule-change` و`operations-automation` لا تحتوي نظامًا داكنًا مماثلًا.
- القرار المطلوب: تحديد ما إذا كان البريد الفعلي سيعتمد الوضعين بعد اختبار Gmail وApple Mail، أم سيبقى فاتحًا مثبتًا مع اقتصار الوضع الداكن على صفحات الويب. لا يُعدّل `buildContractEmail` قبل هذا القرار والموافقة الصريحة لأنه محمي في `AGENTS.md`.

### 11.5 التذييل

- بريد العقد وصفحة تعديل الموعد يستخدمان الاسم الرسمي والروابط الثلاثة المطلوبة.
- رسالة توزيع المصور تستخدم «مأوى للتصوير العقاري» مع البريد والجوال، ولا تعرض الاسم الرسمي وInstagram والموقع كاملًا.
- تذييل العقد المطبوع يعرض «مأوى للتصوير العقاري» بدل الاسم القانوني الكامل.
- القرار المقترح: اعتماد التذييل الموحد الوارد في هذه الوثيقة في كل قناة عند السماح بالتعديل، مع إبقاء رقم الصفحة في العقود عنصرًا منفصلًا.

### 11.6 أصول الشعار

- الصفحات الحديثة والبريد تستخدم أصول `assets/logo2026/`.
- بعض الصفحات القائمة تستخدم `assets/logo.svg` و`assets/logo-mark*.svg` الأقدم.
- ملف PNG المخصص للبريد لا يملك النسبة نفسها لملف SVG الأفقي؛ لا يجوز تبديل أحدهما بالآخر مع إبقاء العرض والارتفاع معًا.
- القرار المقترح: أصول 2026 هي المرجع للأعمال الجديدة، وترحيل الأصول القديمة يكون صفحةً صفحةً بعد معاينة، دون قص أو ضغط.

### 11.7 تنسيق المال

- العقد وبريد العميل يستخدمان أرقامًا إنجليزية عبر `en-US`.
- رسالة توزيع المصور تستخدم حاليًا `Intl.NumberFormat('ar-SA')`، وقد تنتج أرقامًا هندية عربية.
- القرار المقترح: توحيد كل المبالغ المرئية على الصيغة الإنجليزية `1,850 ر.س` مع عزل `LTR`.

## 12. قائمة تحقق قبل اعتماد أي واجهة أو رسالة

- [ ] الخط IBM Plex Sans Arabic فقط، بوزني `400` و`600`.
- [ ] الاسم الرسمي مكتوب «مؤسسة مأوى المهارة التجارية».
- [ ] الأحمر الرسمي `#A4243B` والقيم الأساسية الخمس مستخدمة كما هي.
- [ ] ألوان الحالات صحيحة دلاليًا، ولا تعتمد الحالة على اللون وحده.
- [ ] رقم العقد مطابق للمصدر نفسه في الموقع والبريد والعقد وNotion وSupabase.
- [ ] كل مبلغ بصيغة إنجليزية مثل `1,850 ر.س` ومعزول `LTR`.
- [ ] الجوال والبريد والتاريخ والنسبة ورقم العقد معزولة دون انقلاب أقواس أو ترقيم.
- [ ] الشعار الأصلي غير مقصوص أو مضغوط ونسبته محفوظة.
- [ ] RTL صحيح على الكمبيوتر والهاتف.
- [ ] الوضعان الفاتح والداكن مختبران بحسب قرار القناة المعتمد.
- [ ] Gmail وApple Mail والآيفون والأندرويد والكمبيوتر مختبرة للرسائل المهمة.
- [ ] المعلومة الأساسية والحالة والإجراء التالي مفهومة من أول نظرة.
- [ ] الإيموجي غير مستخدم بإفراط.
- [ ] التذييل الموحد موجود وروابطه قابلة للنقر.
- [ ] لم يتغير منطق الإرسال أو Supabase أو العقود أثناء تطبيق التصميم.
- [ ] عُرضت أي فروق أو قرارات توحيد جديدة على المالك قبل تعديل الملفات القائمة.

