# BLOCKED_DECISIONS — قرارات Product/Business معلّقة (لا توقف التنفيذ)

> كل قرار هنا له Recommended جاهز — وافق أو عدّل، والتنفيذ يتبعه لاحقًا.

---

## 1) ✅ منفّذ (2026-09-16) — واجهة الملاك في admin.html (الخيار 1 الموصى به)

**Question:** اعتماد التغطية والدفعات يحدث الآن عبر استدعاء الدالتين مباشرة (service_key أو JWT مالك). ما الواجهة المفضلة للملاك في Phase 1؟

**المُنفَّذ:** قسم «إيصالات بانتظار الاعتماد» في لوحة التحكم (عرض الإيصال برابط موقّع + زر اعتماد العربون)،
وزر «تأكيد التغطية» في صفوف الطلبات الجديدة — عبر استدعاء coverage-confirm/payment-approve بـJWT الفريق
(يتطلب بريد المالك في employees باسم نشط — «وفاء» موجودة admin ✓).

---

## 1-ق) ~~من يستدعي coverage-confirm وpayment-approve عمليًا؟~~ (أُغلق بالبند أعلاه)

**Question:** اعتماد التغطية والدفعات يحدث الآن عبر استدعاء الدالتين مباشرة (service_key أو JWT مالك). ما الواجهة المفضلة للملاك في Phase 1؟

**Options:**
1. زران في `admin.html` (ملاك مسجلون) يستدعيان الدالتين — تجربة أسرع.
2. استدعاء يدوي من Supabase Dashboard → Edge Functions → Invoke.
3. رابط آمن في إيميل O-03 للملاك (نمط action_tokens مثل العميل).

**Recommended:** الخيار 1 — زر «تأكيد التغطية» في صف الطلب + زر «اعتماد العربون» عند عرض الإيصال في admin.html.

**Impact:** بدون هذا يبقى التأكيد يدويًا تقنيًا (يعمل لكن بلا واجهة).

---

## 2) آلية إرسال C-01 (Quote Ready) الفورية

**Question:** إرسال C-01 فور حفظ العرض من الحاسبة يتطلب استدعاء خادمي (المتصفح لا يملك صلاحية). أي آلية؟

**Options:**
1. Database Webhook على `quotes` (INSERT) → `send-email` — إعداد من اللوحة، صفر كود.
2. تحويل `submit_quote` إلى Edge Function بدل RPC — توحيد لكنه إعادة هيكلة.
3. C-01 عبر الأتمتة المجدولة (تأخير حتى ساعة).

**Recommended:** الخيار 1 — Webhook من اللوحة (Integrations → Webhooks) مع نفس حِمل C-03.

**Impact:** بدون ذلك يصل C-01 متأخرًا حتى ساعة (الخيار 3) أو لا يصل (بلا توصيل).

---

## 3) منطق اقتراح/تعيين المصور

**Question:** `coverage-confirm` يعين حاليًا بالتناوب حسب `sort_order` (round-robin). هل هذا المنطق المطلوب لـ«تعيين محايد»؟

**Options:**
1. Round-robin حسب sort_order (الحالي).
2. حسب عبء العمل (أقل عدد جلسات هذا الشهر).
3. يدوي دائمًا من المالك عند التأكيد.

**Recommended:** الإبقاء على (1) للإطلاق مع إظهار الاسم في واجهة التأكيد للتعديل اليدوي قبل الاعتماد.

**Impact:** منخفض — تغييره لاحقًا لا يمس أي شيء آخر.

---

## 4) IP Rate Limiting (مؤجل بقرار سابق)

**Question:** الحد الحالي 5 طلبات/15 دقيقة لكل رقم جوال — مهاجم يبدّل الأرقام يتجاوزه.

**Options:**
1. Edge Function وسيط أمام `submit_lead/submit_quote` مع تحديد معدل حسب IP.
2. pg_net + جدول عدادات داخل RPC.

**Recommended:** الخيار 1 بعد الإطلاق (Post-Launch Security Improvement — قرار معتمد سابقًا).

**Impact:** مقبول للإطلاق كما هو.

---

## 5) عمود `expires_at` مقابل `valid_until`

**Question:** طلبك السابق ذكر `expires_at`، بينما عمود الصلاحية الفعلي في `quotes` هو `valid_until` (من 013).

**Options:**
1. الإبقاء على `valid_until` (الحالي — مطابق للكود والقوالب).
2. إعادة تسمية إلى `expires_at`.

**Recommended:** الخيار 1 — الاسم واضح، والتغيير يعيد لمس migration منشورة بلا فائدة.

**Impact:** لا شيء وظيفيًا — توثيق فقط.
